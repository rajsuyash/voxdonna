.DS_Store
.claude/
*.mp4
!video/voxdonna-ad.mp4
*.pdf
.env
.gstack/
.lighthouse/
*.bak

# Deep research artifacts (not committed by default)
.research/

# Internal product strategy docs (never deploy to public webroot)
whatsapp_donna/
one-pagers/

# Trainee records for train.voxdonna.com — PII, lives outside the webroot on the
# server and never in git. Created locally by the PHP dev server.
train-data/
